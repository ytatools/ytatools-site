{
  "name": "yta-tools-site",
  "private": true,
  "scripts": {
    "dev": "hugo server -D --config config.development.toml",
    "build": "hugo --gc --minify --config config.production.toml",
    "build:dev": "hugo --gc --minify --config config.development.toml"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.18",
    "postcss": "^8.4.41",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.10"
  }
}
