{
  "name": "yta-tools-site",
  "private": true,
  "scripts": {
    "dev": "hugo server -D --environment development",
    "build": "hugo --gc --minify --environment production",
    "build:dev": "hugo --gc --minify --environment development"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.15",
    "autoprefixer": "^10.4.18",
    "postcss": "^8.4.41",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.10"
  }
}
