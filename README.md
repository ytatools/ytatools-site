# YTA-tools — Hugo Landing Page

## Quick start (Traefik + Docker Compose)
```bash
make setup
make build
make run
```
Then point your domain DNS to the VPS and adjust labels in `docker-compose.yml`.

## Local development
```bash
npm install
npm run dev   # http://localhost:1313
```
